# Example

Demo project using `vite-incra`.

## Setup

```bash
npm install
```

## Scripts

- `npm run dev` - Start dev server
- `npm run build` - Full build
- `npm run build:incremental` - Incremental build (faster for small changes)
- `npm run preview` - Preview production build
